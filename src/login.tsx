import { FormLogin } from "./components/form-login"

export const Login = () => {
  return (
    <div className="bg-black min-h-screen flex items-center justify-center px-12">
      <FormLogin />
    </div>
  )
}
